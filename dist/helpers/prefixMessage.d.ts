import NotificationType from '../types/NotificationType';
declare const prefixMessage: (globalPrefix: string, message: string, notificationType: NotificationType) => string;
export default prefixMessage;
